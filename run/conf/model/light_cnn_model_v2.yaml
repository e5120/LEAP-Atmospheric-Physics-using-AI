name: "LightCNNModelV2"
params: {
  input_size: -1,
  output_size: -1,
  out_channels: 128,   # 256
  kernel_size: 3,
  num_layers: 3,       # 7
  activation: swish,   # relu, eluもあり
  use_aux: False,
  aux_weight: 0.1,
  use_positional_embedding: False,
  num_scalar_feats: 16,
  num_vector_feats: 9,
}
collate_fn: "sequential_collate_fn"
collate_params: {}
