name: "LightCNNModelV2"
params: {
  input_size: -1,
  output_size: -1,
  out_channels: 256,
  kernel_size: 3,
  num_layers: 7,
  activation: swish,
  conv_expand: 4,
  num_se_layers: 3,
  use_positional_embedding: False,
  num_scalar_feats: 16,
  num_vector_feats: 9,
  use_in_aux: False,
  use_aux: False,
  aux_weight: 0.01,
  delta: 1.0,
}
collate_fn: "sequential_collate_fn"
collate_params: {}
